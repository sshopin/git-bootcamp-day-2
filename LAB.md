Сгенерирован `.gitignore` для стека **python** + **visualstudiocode** + **jupyter notebooks**. 
Добавлен `*.swp` для работы в bash.

Настроен nbdime в .gitattributes и .gitconfig.

Тестовая генерация для учебного проекта

